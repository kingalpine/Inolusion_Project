package com.inolusion.rtc.patient_record_system.controllers;

import org.springframework.format.FormatterRegistry;

public interface MVC_Config_2 {
    void addFormatters(FormatterRegistry registry);
}
